Frontend stack: TBD — fill in TEAM_STATE.md once decided (e.g. React + Vite, or Odoo's own frontend tooling).

Rule: this app only calls backend APIs and displays state. No pricing, discount, approval,
stock, or billing math happens here — that's all backend (see API Contracts in TEAM_STATE.md).
